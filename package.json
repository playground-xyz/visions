{
  "name": "knex-wrapper",
  "version": "1.0.0",
  "description": "Small library to allow postgres views to be used and to enable auto join population",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Julian Fell",
  "license": "ISC",
  "dependencies": {
    "join-js": "^0.4.1",
    "lodash": "^4.17.4"
  }
}
